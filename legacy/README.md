# Legacy archive

This directory preserves the pre-revision implementation, reports, and saved
outputs for traceability. It is not part of the active workflow and its results
must not be cited as final evidence.

Use the package root's `README.md`, `README_FA.md`, and `scripts/` directory
for the current task-disjoint implementation.
