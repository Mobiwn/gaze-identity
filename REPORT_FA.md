# شناسایی هویت با نگاه در وظایف ویدئویی مجزا

**تهیه‌کننده:** Mobin Kheibary  
**استاد راهنما:** Dr. Shiva Kamkar

## مسئله و داده

هدف این پروژه بررسی این پرسش است: آیا الگوی نگاه یک شرکت‌کنندهٔ شناخته‌شده، در
وظیفه‌ای که مدل در آموزش ندیده است نیز قابل تشخیص باقی می‌ماند؟ دادهٔ استفاده‌شده
مختصات دوبعدی ردیابی چشم از فایل‌های محلی EasyCog است. این تحلیل صرفاً بر دادهٔ
محلی و شناسایی هویت در مجموعهٔ بسته متمرکز است و هدف بالینی ندارد.

واحد تحلیل، قطعه‌های تصویری ذخیره‌شده در `taskN_pic` است. برخلاف نسخهٔ قدیمی،
وظیفه به‌طور مصنوعی به ده قطعهٔ مساوی تقسیم نمی‌شود؛ به‌ویژه وظیفهٔ 6 که در
دادهٔ محلی شش قطعهٔ تصویری دارد. به دلیل ناسازگاری شماره‌گذاری در منابع محلی،
نام وظایف در این گزارش همان `task1` تا `task9` است و برچسب‌های شناختی بدون تأیید
منبع اصلی به آن‌ها نسبت داده نمی‌شود.

## روش

برای هر فرد، نخستین جلسهٔ معتبر با سیاست ثبت‌شده در پیکربندی انتخاب می‌شود.
مختصات گمشده فقط هنگامی که دست‌کم دو مقدار معتبر وجود داشته باشد، به‌صورت خطی
درون‌یابی می‌شوند. قطعه‌های نامعتبر یا کم‌کیفیت به‌جای پنهان شدن، همراه دلیل در
`segment_exclusions.json` ثبت می‌شوند.

از هر قطعه ویژگی‌های مکانی، سرعت، شتاب، خلاصهٔ تثبیت و ساکاد، طول مسیر نگاه و
آنتروپی فضایی ساخته می‌شود. شبکه‌های ناحیهٔ توجه (AOI) عمداً استفاده نشده‌اند؛
زیرا حدود صفحه و معنای دقیق مختصات هنوز مستقلاً تأیید نشده است.

تقسیم‌بندی پیش‌فرض کاملاً مجزا است: `task1` و `task2` برای آموزش، `task3` برای
انتخاب مدل، و `task7` و `task8` برای آزمون نهایی. سه مدل ساده مقایسه شدند:
nearest centroid و دو Logistic Regression با منظم‌سازی متفاوت. انتخاب مدل فقط
با دادهٔ اعتبارسنجی انجام شد. حذف ویژگی‌های با واریانس صفر و استانداردسازی نیز
فقط از دادهٔ آموزش یاد گرفته شدند.

## نتیجهٔ اجرای بازبینی‌شده

اجرای ذخیره‌شده در `artifacts/task_disjoint_baseline/results.json` پس از کنترل
قطعه‌ها، 42 شرکت‌کنندهٔ مشترک را در سه بخش حفظ کرد. تعداد ردیف‌های ویژگی برای
آموزش، اعتبارسنجی و آزمون به‌ترتیب 813، 409 و 829 بود. مدل انتخاب‌شده Logistic
Regression با `C=0.1` بود.

در وظایف کاملاً نگه‌داشته‌شدهٔ آزمون، دقت top-1 برابر **26.66٪** و احتمال تصادفی
در مجموعهٔ بسته **2.38٪** بود. دقت top-5 برابر 59.35٪ و میانگین reciprocal rank
برابر 0.417 بود. بازهٔ اطمینان bootstrap در سطح شرکت‌کننده برای دقت top-1 از
19.37٪ تا 34.53٪ به‌دست آمد. مقدار p آزمون permutation برچسب افراد، با 1,000
تکرار و همین تنظیمات، 0.000999 بود.

این اعداد نتیجهٔ همان پیکربندی، همان فایل‌های محلی و همان cohort هستند. آن‌ها
اثبات‌کنندهٔ اثر علّی بار شناختی، عملکرد در جلسهٔ دیگر، یا عملکرد در جامعهٔ جدید
نیستند.

## محدودیت‌ها و گام بعدی

نگاشت دقیق شناسهٔ وظایف محلی به نام و سطح شناختی باید با منبع اصلی تأیید شود.
جلسه‌های تکراری بعضی افراد باید در تحلیلی مستقل برای پایداری زمانی بررسی شوند.
همچنین پیش از تفسیر سرعت بر حسب پیکسل یا استفاده از AOI، باید رزولوشن صفحه،
واحد مختصات و قرارداد نمونه‌های نامعتبر بررسی شود. در ادامه لازم است تحلیل با
تقسیم‌بندی‌های ازپیش‌تعیین‌شدهٔ دیگر و ترجیحاً یک cohort مستقل تکرار شود.

## مرجع

Hu et al. (2026). *The EasyCog Dataset: Towards Easier Cognitive Assessment
with Passive Video Watching*. Proceedings of the ACM on Interactive, Mobile,
Wearable and Ubiquitous Technologies, 10(1), Article 4.
https://doi.org/10.1145/3789682
